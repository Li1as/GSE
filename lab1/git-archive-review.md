# Git 存档前检查

日期：2026-09-15。范围：阶段一 A—D、阶段二 2A—2E 的代码、测试、设计与验收记录。

## 私有信息处理

- 本地 API 和邮箱配置、个人资料、任务数据库、访问口令、邮件与附件、运行记录及备份均被忽略。
- 设计与验收文档中的姓名、个人来源路径、成绩、毕业安排、真实任务编号、邮件元信息和内网地址已脱敏；保留功能结论和验证边界。
- 两个真实验收脚本中的个人预期值移至 `data/` 下的本地 JSON，公开示例使用虚构值；离线成绩样例亦使用替代值。
- Windows 转发脚本的地址改为必填参数。
- 脱敏前的相关文档与脚本保存在被忽略的 `data/private-archive/pre-git/`，真实资料与业务数据库未改写。
- 本次提交使用通用作者署名；既有 Git 历史不改写。本次仅本地提交，不推送远程。

## 本轮验证

- 107 项离线单元测试通过；模型和 SMTP 使用模拟对象。
- 真实查询验收判定函数在外置预期值前后的 18 项离线比较一致。
- Python 语法、JSON 解析、JavaScript 语法和 Git 空白检查通过。
- 对候选文件比对本地已知凭据、联系方式与私有标识，并检查密钥模式和运行产物；未发现残留匹配。检查输出不包含真实凭据。
- 验证个人资料、两份本地配置、访问口令、私有预期值、备份、SQLite 和 EML 的忽略规则。

本轮没有调用真实 API、访问邮箱或发送邮件。其他验收文档中的真实服务结果为历史记录。本次检查针对待提交内容，不构成对既有提交历史或任意未知敏感内容的全面保证。
